name: rust-toy-app
on: [push, pull_request]
jobs:
  build-release:
    permissions:
      contents: read
    env:
      BUILD_PROFILE: debug
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          # - runner: ubuntu-latest
          #   target: aarch64-unknown-linux-gnu # TODO: re-introduce when implemented
          # - runner: ubuntu-latest
          #   target: x86_64-unknown-freebsd # TODO: re-introduce when implemented
          # - runner: ubuntu-latest
          #   target: aarch64-unknown-freebsd # TODO: re-introduce when implemented
          # - runner: ubuntu-latest
          #   target: x86_64-unknown-openbsd # TODO: re-introduce when implemented
          # - runner: ubuntu-latest
          #   target: aarch64-unknown-openbsd # TODO: re-introduce when implemented
          # - runner: macos-latest
          #   target: x86_64-apple-darwin # TODO: re-introduce when implemented
          # - runner: macos-latest
          #   target: aarch64-apple-darwin # TODO: re-introduce when implemented
          # - runner: windows-latest
          #   target: x86_64-pc-windows-msvc # TODO: re-introduce when implemented
          # - runner: windows-latest
          #   target: aarch64-pc-windows-msvc # TODO: re-introduce when implemented
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
      - name: Build
        uses: ./.github/actions/rust-build-release
        with:
          target: ${{ matrix.target }}
          project-dir: rust-toy-app
      - name: Verify artifacts
        run: |
          test -f target/${{ matrix.target }}/release/rust-toy-app
          ls target/${{ matrix.target }}/release/build/rust-toy-app-*/out/rust-toy-app.1
        working-directory: rust-toy-app
