[package]
name = "rust-toy-app"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
publish = false

[dependencies]
clap = { version = "4", features = ["derive"] }

[build-dependencies]
clap = { version = "4", features = ["derive"] }
clap_mangen = "0.2"
time = { version = "0.3", features = ["formatting", "macros"] }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
rstest = "0.21"
glob = "0.3"
